## 1. Implementation
- [x] 1.1 Select a Rust HTTP stack that supports TLS-PSK and document the choice in `design.md`.
- [x] 1.2 Create a service binary with a `GET /hello` handler and env-based configuration for host/port and TLS-PSK.
- [x] 1.3 Create a separate CLI binary that prints `Hello, world`.
- [x] 1.4 Add unit tests for the service handler and the CLI output.
- [x] 1.5 Add minimal docs on running the service with and without TLS-PSK.

## 2. Validation
- [x] 2.1 Run `cargo test`.
