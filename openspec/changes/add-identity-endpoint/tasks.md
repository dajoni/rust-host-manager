## 1. Implementation
- [x] 1.1 Add API helpers for identity data and URL building.
- [x] 1.2 Retrieve the service binary version for the identity response.
- [x] 1.3 Add `/v1/identity` handler to the service with JSON response.
- [x] 1.4 Add tests for identity helpers and handler response (including JSON content type).
- [x] 1.5 Update documentation or README with the new endpoint.
- [x] 1.6 Update the CLI to call `/v1/identity` and display the service version.

## 2. Validation
- [ ] 2.1 Run `cargo test`.
